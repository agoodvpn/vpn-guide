---
title: VPN分流是什么？全局模式和分流模式怎么选
description: VPN分流会让部分流量走VPN、部分流量直连。搞清楚Split Tunneling和全局模式的区别，才能避免IP没变、速度慢和应用冲突。
---

# VPN 分流是什么？

VPN 分流（Split Tunneling）简单说就是：**不是所有流量都走 VPN。** 你可以让某些 App、网站或网段进入 VPN，其他流量继续走原来的网络。

这听起来像一个高级功能，其实日常很实用。比如工作后台必须走公司 VPN，但视频会议和本地打印机没必要绕公司网络；又或者某个 App 需要指定地区节点，而其他应用继续直连。

## 分流和全局模式，差别就在“谁走隧道”

| 模式 | 流量怎么走 | 常见优点 | 常见问题 |
|---|---|---|---|
| 全局 / 全隧道 | 基本所有流量走 VPN | 行为简单、出口统一 | 更吃带宽，部分本地服务可能受影响 |
| 分流 | 只有指定流量走 VPN | 速度和兼容性更灵活 | 配错规则后容易出现“有的 App 换 IP、有的没换” |

[Microsoft 的 Windows VPN 路由文档](https://learn.microsoft.com/en-us/windows/security/operating-system-security/network-security/vpn/vpn-routing)也把 split tunnel 和 force tunnel 分开处理：前者只有指定路由进入 VPN，后者默认把全部流量送进 VPN。

## 家用场景为什么会用到分流？

一个很实际的例子：电视看 4K 视频时，希望流媒体 App 走某个节点，但手机银行、智能家居或本地 NAS 不希望绕远路。

分流可以减少不必要的路径，也能降低全局 VPN 对本地设备发现、局域网访问和某些 App 的影响。

但如果你并不清楚规则怎么配置，先用全局模式反而更省心。分流最大的坑不是“功能不好”，而是**你忘了自己分过流**。

## 为什么会出现“VPN连上了，IP却没变”？

很多时候就是因为你正在查看的浏览器没有被分到 VPN 里。

例如：

- 只有游戏客户端走 VPN，浏览器直连；
- 只有公司网段走 VPN，普通网站直连；
- 某个 App 被加入了绕过列表。

这类情况并不是 VPN 失效。具体排查可以看 [VPN 连上但 IP 没变怎么办](vpn-connected-but-ip-not-changed.html)。

## 远程办公里，分流更常见

企业环境里，分流经常被用来减少 VPN 网关压力。Microsoft 也公开提供了面向 Microsoft 365 流量的 VPN 优化方案，让部分云服务流量走更直接的路径，而不是全部绕回公司网络再出去。

这类配置一般由 IT 管理员统一下发。普通员工不要自己修改公司的路由或安全策略，尤其是涉及内网、条件访问和数据防护时。

如果你的重点是远程办公，可以看 [远程办公用 VPN 要关注什么](vpn-for-remote-work.html)。

## 分流会不会降低安全性？

它不会自动“不安全”，但安全边界会变复杂。

全局模式下，出口相对统一；分流后，你必须知道哪些 App 受 VPN 保护、哪些仍走本地网络。对公司设备来说，这通常需要明确策略，而不是让每个人随意配置。

日常个人使用则简单很多：对隐私敏感的应用放进 VPN，本地服务和不需要绕路的应用留在直连即可。

## 什么时候不建议开分流？

有几种情况我更倾向于先关掉：

- 你正在排查 IP、DNS 或连接异常；
- 你不知道当前规则是谁设置的；
- 某个关键应用必须确保全部流量经过 VPN；
- 你发现同一台设备不同 App 显示不同地区。

先用全局模式把主连接验证正常，再逐步加规则，会比直接在复杂分流里找问题快很多。

如果还担心 VPN 意外断开后的流量去向，可以继续看 [Kill Switch 是什么](vpn-kill-switch.html)。
